# Claude Code に投げるプロンプト集

このファイルにある文言をそのまま Claude Code にコピペすると、
追加機能や修正をスムーズに実装してもらえます。

## 🚀 最初に投げるプロンプト

```
このプロジェクトは Google スプレッドシートと連携した在庫管理 Web アプリです。
CLAUDE.md と SETUP.md を読んで、プロジェクトの構成を把握してください。
そのあと、私が SETUP.md の手順を踏めるよう一緒に進めてください。
スプレッドシート ID と Apps Script Web App URL は私が手で設定します。
```

## 🔧 動作確認

```
web/ ディレクトリで簡易 HTTP サーバーを起動して、
ブラウザで開いて動作を一緒に確認したいです。
ローカル確認の手順を教えてください。
```

## ✨ 機能追加のサンプルプロンプト

### 賞味期限アラートを追加

```
賞味期限が今日から7日以内の商品を黄色でハイライトして、
画面上部のアラートバーに「期限間近: X件」と表示する機能を追加してください。
GAS と フロント両方の修正が必要です。
```

### CSV エクスポート

```
履歴を CSV でダウンロードできる機能を追加してください。
履歴タブにダウンロードボタンを置いて、押すと現在表示中の履歴を CSV にして保存します。
ブラウザだけで完結する実装(GAS 不要)でお願いします。
```

### バーコード読み取り

```
スマホのカメラで商品コードのバーコードを読み取って、
該当商品のモーダルを開く機能を追加してください。
ライブラリは ZXing-js を CDN から使ってください。
```

### LINE 通知

```
最低在庫を下回ったとき、LINE Notify に通知を送る機能を追加してください。
GAS のトリガーで毎朝9時に在庫をチェックし、
該当商品を 1 通のメッセージにまとめて送ります。
LINE Notify のトークンは設定シートに置きます。
```

### 操作者をパスコードで切替

```
4桁のパスコードでユーザー切替する機能を追加してください。
設定シートに operator_codes として `田中:1234,鈴木:5678` のように保存し、
ヘッダーの操作者欄をタップしたら数字パッドが出てくるようにしてください。
```

### 月別の入出庫グラフ

```
新しいタブ「📊 分析」を追加して、
過去30日の入庫・出庫数を商品別に積み上げ棒グラフで表示してください。
Chart.js を CDN から使い、フロント側で集計します。
```

### 写真添付

```
入出庫モーダルに「写真を撮る」ボタンを追加してください。
撮った写真は Google Drive にアップロードし、
履歴シートの備考列にリンクを残します。
GAS の DriveApp を使います。
```

## 🐛 デバッグ用プロンプト

### API が動かない

```
ブラウザのコンソールに以下のエラーが出ています:
[エラーメッセージを貼る]
原因と対処を教えてください。
```

### スプレッドシートが更新されない

```
入庫ボタンを押しても在庫が更新されません。
GAS の実行ログの見方と、よくある原因を教えてください。
```

## 🎨 デザイン調整

```
全体の色調を「夜の屋台」風に変えてください。
- 背景: 深い藍色
- アクセント: 提灯のような橙
- フォント: もう少し手書き風に
```

```
スマホ専用にレイアウトを最適化してください。
- ヘッダーをもっと薄く
- ボタンをもっと大きく(指で押しやすく)
- カテゴリータブを横スクロールではなくグリッドに
```
