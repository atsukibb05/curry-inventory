# セットアップ手順 (約 30 分)

このとおりにやれば動きます。Google アカウントだけ用意してください。

## ステップ 1: スプレッドシートを作る

1. [Google ドライブ](https://drive.google.com) で **新規 → Google スプレッドシート**
2. ファイル名を `カレー在庫管理` などに変更
3. URL の `/d/` と `/edit` の間にある長い文字列が **スプレッドシート ID** です
   ```
   https://docs.google.com/spreadsheets/d/【ここがID】/edit
   ```
   この ID をメモしておく

## ステップ 2: Apps Script を貼る

1. スプレッドシートのメニューで **拡張機能 → Apps Script** をクリック
2. 開いたエディタの `Code.gs` の中身を全部消して、本プロジェクトの `gas/Code.gs` の内容を貼り付け
3. 上の方にある定数 `SPREADSHEET_ID` を、ステップ 1 でメモした ID に書き換える
   ```javascript
   const SPREADSHEET_ID = '1AbCdEfGhIj...あなたのID...XyZ';
   ```
4. 💾 保存

## ステップ 3: シートを初期化する

Apps Script のエディタで、関数選択ドロップダウンから `initializeSheets` を選んで **▶ 実行**

- 初回は権限の確認ダイアログが出ます
- 「詳細」→「(プロジェクト名)に移動(安全ではないページ)」→「許可」
  (自分が自分のスプレッドシートを操作するための承認なので問題なし)
- 実行ログに「初期化完了」と出れば OK
- スプレッドシートを見ると 4 つのシート(在庫マスタ・履歴・カテゴリー・設定)とサンプル 10 商品が入っています

## ステップ 4: Web アプリとしてデプロイ

1. Apps Script 画面の右上 **デプロイ → 新しいデプロイ**
2. ⚙ **種類の選択** → **ウェブアプリ**
3. 設定:
   - **説明**: `カレー在庫 v1` など何でも OK
   - **次のユーザーとして実行**: **自分**
   - **アクセスできるユーザー**: **全員**
4. **デプロイ**
5. 表示される **ウェブアプリの URL** をコピー
   ```
   https://script.google.com/macros/s/AKfycb.../exec
   ```

## ステップ 5: フロントに URL を貼る

`web/app.js` の冒頭:

```javascript
const API_URL = 'https://script.google.com/macros/s/XXXXXXXXXXXXXXXXXX/exec';
```

を、ステップ 4 でコピーした URL に書き換えて保存。

## ステップ 6: 起動する

### ローカルで確認(おすすめ)
ターミナルで:
```bash
cd web
python3 -m http.server 8000
```
ブラウザで `http://localhost:8000` を開く。

### スマホで使うには

スマホからアクセスするには、`web/` の中身をどこかに公開する必要があります。
無料の選択肢:

- **GitHub Pages**: リポジトリの Settings → Pages から公開
- **Cloudflare Pages**: GitHub と連携してビルドなしで公開
- **Netlify Drop**: `web/` フォルダをドラッグ&ドロップでデプロイ
- **Vercel**: `vercel deploy` で公開

公開した URL をスマホのホーム画面に追加すると、アプリのように使えます。

## ステップ 7: 動作確認

1. 在庫一覧が表示される ✅
2. カテゴリータブを切り替えられる ✅
3. 商品をタップしてモーダルが開く ✅
4. 入庫ボタンで在庫が増える ✅
5. 履歴タブに記録が残る ✅
6. スプレッドシート側にも反映されている ✅
7. 最低在庫を下回ると赤くハイライト + アラートバー表示 ✅

---

## バックエンド更新のしかた

`Code.gs` を編集したら:
- Apps Script 画面で **デプロイ → デプロイを管理 → ✏ → バージョン: 新バージョン → デプロイ**
- これで URL は同じまま、コードだけ更新される(`web/app.js` の URL は変えなくてよい)

## トラブルシューティング

### 「読み込み失敗」と出る
- `web/app.js` の `API_URL` が正しいか
- ブラウザのコンソール(F12)でエラーを確認
- API URL を直接ブラウザで開いて `?action=list` を付けると JSON が返るはず:
  `https://script.google.com/.../exec?action=list`

### 「権限がありません」エラー
- ステップ 4 の「アクセスできるユーザー: 全員」になっているか確認
- 「次のユーザーとして実行: 自分」になっているか確認

### CORS エラーが出る
- POST が `text/plain` で送られているか(`app.js` の `apiPost` を確認)
- 別オリジン環境でも動くよう設計されているので通常は問題なし

### 在庫が更新されない
- スプレッドシート側が他の人に編集ロックされていないか
- Apps Script の実行ログを確認(`表示 → 実行数`)
