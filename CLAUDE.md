# Claude Code 用プロジェクト情報

## このプロジェクトについて

カレー食品加工事業者向けの在庫管理 Web アプリです。
バックエンドは Google Apps Script、データ保存は Google スプレッドシート、
フロントは静的 HTML/CSS/JS(フレームワーク不使用)で構成しています。

## ディレクトリ構成と役割

- `gas/Code.gs` - Apps Script のメインコード。Web App としてデプロイし、
  `doGet`/`doPost` で JSON API を提供する。
- `gas/appsscript.json` - GAS のマニフェスト。タイムゾーンは Asia/Tokyo。
- `web/index.html` - SPA のエントリーポイント。タブ切替で在庫一覧・履歴・追加を表示。
- `web/styles.css` - レスポンシブ。スマホ縦持ち優先(min-width: 320px)。
- `web/app.js` - Apps Script の Web App URL を `API_URL` 定数に設定して使う。
  fetch でバックエンドと通信、JSON でやり取り。

## 開発のお願い

- **バックエンド変更時** は GAS エディタで再デプロイ(新しいバージョン)が必要。
  デプロイ URL は変えなくて済むよう「既存デプロイの管理」から更新する。
- **API 仕様** は `Code.gs` の冒頭コメントを正としてください。
- **シート列の順序** を変えるときは `Code.gs` の定数 `COL_*` も合わせて更新する。
- **ローカル動作確認** は `web/` ディレクトリで `python3 -m http.server` でも OK。
- CORS 対策で GAS 側は `ContentService.MimeType.JSON` を返している。
- **新機能追加時** は履歴シートに記録する処理を忘れずに(`logHistory()` を呼ぶ)。

## データモデル

### 在庫マスタ シート (`stock`)
| 列 | 名前 | 型 | 例 |
|---|---|---|---|
| A | 商品コード | string | SP001 |
| B | 商品名 | string | クミンパウダー |
| C | カテゴリー | string | 香辛料 |
| D | 単位(容器) | string | g |
| E | 現在庫 | number | 500 |
| F | 最低在庫 | number | 200 |
| G | 賞味期限 | date | 2026-12-31 |
| H | 備考 | string | - |
| I | 更新日時 | datetime | 自動 |

### 履歴 シート (`history`)
| 列 | 名前 | 例 |
|---|---|---|
| A | 日時 | 2026-04-25 14:30:00 |
| B | 操作者 | 田中 |
| C | 商品コード | SP001 |
| D | 商品名 | クミンパウダー |
| E | 区分 | 入庫/出庫 |
| F | 数量 | 100 |
| G | 操作後在庫 | 600 |
| H | 備考 | 仕入先A |

### カテゴリー シート (`category`)
| 列 | 名前 | 例 |
|---|---|---|
| A | カテゴリー名 | 香辛料 |
| B | 表示順 | 1 |
| C | アイコン | 🌶️ |

### 設定 シート (`config`)
| 列 | キー | 値 |
|---|---|---|
| A | alert_threshold_ratio | 1.0 |
| A | default_operator | 田中 |

## よくある作業

- 商品を追加: 在庫マスタシートに行を足すか、Web 画面の「商品追加」フォームから
- カテゴリー追加: カテゴリーシートに行を足す
- ユーザー追加: 設定シートまたはログイン機能を後追加(現状は操作者名を選ぶだけ)
- アラート閾値変更: 設定シートの `alert_threshold_ratio` を変更
